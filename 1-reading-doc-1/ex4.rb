=begin

"Break" is under Class: Object.
It causes unconditional termination
of a code/while/until block, with
control transferred to the line
after the block.

If given an argument, it returns
that argument as the value
of the terminated block.

Returns "nil" otherwise.
